import 'package:flutter/material.dart';

class ProfileSummaryPerformanceHeader extends StatelessWidget {
  const ProfileSummaryPerformanceHeader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
