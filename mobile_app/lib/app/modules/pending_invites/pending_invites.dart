import 'package:flutter/material.dart';

import 'views/pending_invites_mobile.dart';

class PendingInvitesScreen extends StatelessWidget {
  const PendingInvitesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const PendingInvitesMobile();
  }
}
